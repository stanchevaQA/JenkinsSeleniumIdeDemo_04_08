pipeline {
    agent any

    stages {
        stange("Checkout code") {
            //checkout the repository
            steps {
                git branch: "main", url: "https://github.com/stanchevaQA/JenkinsSeleniumIdeDemo_04_08"
            }
        }
        stange("Set up .Net Core") {
            //install dot net
        }
        stange("Restore depenedencies") {
            //install dependencies
        }
        stange("Build") {
            //build
        }
        stange("Run Tests") {
            //test
        }
    }

}